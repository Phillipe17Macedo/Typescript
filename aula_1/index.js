const soma = (a, b) => {
    console.log(a + b);
};

soma (2, 5);
soma ('Phillipe', 'Macedo'); //Se observar, o Javascript não tipifica a variável como no Typescript, por isso ele concatena as strings.
// Para uma aplicação mais robusta, não é muito recomendado Js por esse motivo, tipificação de variaveis


